import { DecimaldatePipe } from './decimaldate.pipe';

describe('DecimaldatePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimaldatePipe();
    expect(pipe).toBeTruthy();
  });
});
